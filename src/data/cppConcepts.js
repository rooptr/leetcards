const concept = (term, definition, example) => ({ term, definition, example });

export const cppConcepts = {
  'cpp-build': [
    concept('Translation unit', 'One source file after preprocessing, including the text of every included header. Each translation unit is compiled independently.', 'main.cpp and every header it includes become one translation unit.'),
    concept('Object file', 'The compiler output containing machine code, symbols, relocation records, and debugging information that is not yet a complete program.', 'main.o may contain an unresolved reference to a function defined in math.o.'),
    concept('Linker', 'The tool that resolves cross-file symbols, applies relocations, combines libraries, and emits the final executable or firmware image.', 'Omitting math.o produces an undefined-reference linker error.'),
    concept('One-definition rule', 'The C++ rule that requires program entities to have the permitted number of mutually consistent definitions across translation units.', 'Different macro-controlled class definitions in two files can violate the rule without a useful diagnostic.'),
    concept('CMake', 'A build-system generator that describes targets, sources, include paths, compile features, definitions, and linked libraries, then emits build files for a selected backend.', 'target_compile_features(app PRIVATE cxx_std_20) records the language requirement on one target.'),
  ],
  'cpp-namespaces': [
    concept('Namespace', 'A named scope that groups declarations and prevents unrelated libraries from colliding on the same short identifier.', 'telemetry::Packet and diagnostics::Packet can coexist.'),
    concept('Linkage', 'The rule that determines whether declarations in different scopes or translation units denote the same entity.', 'An unnamed-namespace variable has internal linkage and is private to one translation unit.'),
    concept('Inline', 'A function or variable specifier that permits identical definitions in multiple translation units; it is not a command to perform call-site expansion.', 'An inline variable defined in a header denotes one program-wide object.'),
    concept('Argument-dependent lookup', 'Additional name lookup in namespaces associated with a function call’s argument types.', 'A free swap declared beside a type can be found by an unqualified swap(valueA, valueB) call.'),
  ],
  'cpp-modern-syntax': [
    concept('auto', 'A placeholder that asks the compiler to deduce a variable’s type from its initializer while preserving the normal deduction rules for references and constness.', 'auto value = 4 deduces int; auto& reference = value deduces int&.'),
    concept('decltype', 'A compile-time operator that reports the type of an expression using rules that preserve value category for parenthesized expressions.', 'decltype(value) may be int, while decltype((value)) is int& for an lvalue variable.'),
    concept('nullptr', 'The dedicated null pointer literal. It converts to pointer types without also behaving like the integer zero in overload resolution.', 'An overload set containing f(int) and f(char*) selects f(char*) for nullptr.'),
    concept('enum class', 'A scoped enumeration whose enumerators do not leak into the surrounding scope and do not implicitly convert to integers.', 'State::ready is distinct from an unrelated Mode::ready.'),
    concept('Range-based for', 'Loop syntax that obtains begin and end from a range and visits each element in sequence.', 'for (const auto& sample : samples) avoids copying each sample.'),
    concept('Structured binding', 'Syntax that introduces names for elements of an array, tuple-like object, or accessible data members.', 'auto [iterator, inserted] = table.emplace(key, value).'),
    concept('inline function', 'A function that may be defined identically in several translation units. The compiler may still choose either to inline or not inline its machine code.', 'A small header-defined accessor can be inline without promising call-site expansion.'),
    concept('inline variable', 'A C++17 variable definition that may appear identically in several translation units while representing one entity.', 'inline constexpr auto timeout = 50ms can live in a header.'),
  ],
  'cpp-object-model': [
    concept('Object', 'A region of storage with a type and a lifetime. Existing bytes do not automatically imply that an object of a desired type is alive there.', 'Placement construction begins an object’s lifetime in supplied storage.'),
    concept('Lifetime', 'The interval during which an object exists and may be accessed according to its type and invariant.', 'A pointer becomes dangling when the pointed object’s lifetime ends even if its bytes remain unchanged.'),
    concept('Value semantics', 'The contract that defines copying, moving, comparison, and independent ownership for values of a type.', 'Copying a string produces an independently usable string value.'),
    concept('Moved-from state', 'The valid but usually unspecified state left after an object’s resources have been transferred by a move operation.', 'A moved-from string may be assigned to or destroyed, but its previous text cannot be assumed.'),
  ],
  'cpp-encapsulation': [
    concept('Encapsulation', 'The practice of keeping representation and invariant-preserving operations together so callers cannot create invalid object states directly.', 'A Counter exposes increment() instead of allowing arbitrary writes to its count.'),
    concept('public', 'Members in the public section form the interface available to every caller that can name the object.', 'A public size() operation reports state without exposing the storage representation.'),
    concept('private', 'Members in the private section are accessible only to the class and its friends, allowing the class to control its invariant.', 'A private balance cannot be set negative without passing through a checked operation.'),
    concept('protected', 'Members in the protected section are accessible to the class, its friends, and derived classes; using it for mutable data tightly couples derived types to representation.', 'A protected hook function is safer than a protected raw data field.'),
    concept('Data hiding', 'Preventing callers from depending on representation details that may change without altering the public contract.', 'Changing storage from an array to a vector does not affect users of push() and size().'),
    concept('Getter', 'A query operation that returns selected state without granting uncontrolled mutation; it should express a meaningful domain question rather than mirror every field automatically.', 'temperature_celsius() is a domain query, while raw mutable access to sensor registers is not.'),
    concept('Setter', 'A mutation operation that validates a requested change and preserves the object invariant before committing it.', 'set_baud_rate rejects unsupported rates before changing configuration.'),
  ],
  'cpp-constructors': [
    concept('Default constructor', 'A constructor callable with no arguments. It establishes the type’s default valid state and may be compiler-generated, explicitly defaulted, or user-written.', 'Session() creates a disconnected but valid session.'),
    concept('Parameterized constructor', 'A constructor that receives values needed to establish a particular initial state.', 'Buffer(std::size_t capacity) allocates the requested capacity.'),
    concept('Copy constructor', 'A constructor that creates a new object from an existing object of the same type, normally using a const reference parameter.', 'Buffer(const Buffer& other) duplicates owned bytes.'),
    concept('Move constructor', 'A constructor that creates an object by transferring resources from an rvalue of the same type, leaving the source valid but unspecified.', 'Buffer(Buffer&& other) noexcept takes the allocation pointer.'),
    concept('Delegating constructor', 'A constructor whose initializer list invokes another constructor of the same class so initialization policy has one implementation.', 'Widget() : Widget(0, "idle") {} delegates to the parameterized constructor.'),
    concept('Conversion constructor', 'A non-explicit constructor callable with one effective argument that permits implicit conversion from the argument type to the class type.', 'Duration(int) can make a Duration from an int unless marked explicit.'),
    concept('explicit constructor', 'A constructor marked explicit cannot be used for unintended implicit conversion or copy-initialization, but remains available for direct initialization.', 'explicit Duration(int) allows Duration{5} and rejects pass_duration(5).'),
    concept('Deleted constructor', 'A constructor declared with = delete participates in overload resolution but makes the selected construction form ill-formed.', 'Device(const Device&) = delete prevents copying a unique hardware owner.'),
    concept('Defaulted constructor', 'A constructor declared with = default asks the compiler to generate the operation under the normal memberwise rules.', 'Record() = default preserves simple default construction when another constructor exists.'),
    concept('Trivial destructor', 'A destructor that performs no user-defined cleanup and whose bases and members also have trivial destructors; objects of such types need no destruction work.', 'A plain aggregate of integers normally has a trivial destructor.'),
    concept('Non-trivial destructor', 'A destructor that must run because it is user-provided, virtual, or a base/member requires destruction.', 'A vector member makes the containing type’s destructor non-trivial.'),
    concept('Virtual destructor', 'A virtual base-class destructor that makes deleting through a base pointer dispatch to the complete derived object’s destructor.', 'Deleting unique_ptr<Base> safely destroys Derived only when Base has a virtual destructor.'),
    concept('Defaulted destructor', 'A destructor declared with = default that retains compiler-generated memberwise destruction while making the intent explicit.', '~Record() = default destroys members in reverse declaration order.'),
  ],
  'cpp-copy-move': [
    concept('Rule of three', 'If a type manually defines a destructor, copy constructor, or copy assignment operator because it owns a resource, it usually needs all three to keep copying and cleanup consistent.', 'A raw owning pointer needs deep copy construction, deep copy assignment, and one matching delete.'),
    concept('Rule of five', 'In modern C++, a resource-owning type that defines the rule-of-three operations may also need a move constructor and move assignment operator.', 'A buffer transfers its pointer during vector reallocation instead of copying every byte.'),
    concept('Rule of zero', 'Prefer members that already manage their own resources so the containing class defines none of the five special ownership operations.', 'A class containing vector and string usually needs no custom destructor, copy, or move code.'),
    concept('Copy assignment', 'An operation that replaces the value of an already-live object from another object while preserving correctness under self-assignment and failure.', 'destination = source must release or replace destination’s previous resource safely.'),
    concept('Move assignment', 'An operation that releases an object’s current resource, transfers another object’s resource into it, and leaves the source valid.', 'destination = std::move(source) transfers ownership into an existing destination.'),
    concept('std::move', 'A cast that turns its argument into an xvalue so move-enabled overloads may be selected; it performs no transfer by itself.', 'std::move(buffer) permits a move constructor to take buffer’s resource.'),
    concept('Moving from const', 'A const rvalue usually cannot bind to a move operation that must modify its source, so an available copy operation is commonly selected instead.', 'std::move(const_string) normally copies because string(string&&) needs a non-const rvalue.'),
  ],
  'cpp-const': [
    concept('Const object', 'An object whose observable value cannot be modified through that access path; only const-qualified operations are available through it.', 'A const Buffer may expose size() but not resize().'),
    concept('Const member function', 'A member function whose implicit object parameter is const-qualified and therefore cannot modify ordinary data members.', 'std::size_t size() const can be called on const and non-const objects.'),
    concept('mutable', 'A member specifier allowing carefully chosen bookkeeping to change inside a const operation without changing the logical value.', 'A cached hash or synchronization mutex may be mutable.'),
    concept('Const propagation', 'The design discipline of carrying read-only intent through parameters, references, pointers, iterators, and return values.', 'A const container returns const references from element access.'),
  ],
  'cpp-references': [
    concept('lvalue', 'An expression that identifies an object or function with continuing identity and can normally appear on the left side of assignment when non-const.', 'A named variable is an lvalue even when its declared type is T&&.'),
    concept('rvalue', 'A temporary value or expiring object whose resources may be reused because the expression does not promise continuing identity to the caller.', 'A temporary string returned from a function is an rvalue.'),
    concept('lvalue reference', 'A T& alias that normally binds to lvalues, preserves the referred object’s identity, and does not extend ownership or lifetime.', 'void inspect(const Packet&) reads an existing packet without copying it.'),
    concept('rvalue reference', 'A T&& alias that normally binds to rvalues and enables operations that may reuse their resources.', 'Buffer(Buffer&&) can take ownership from a temporary buffer.'),
    concept('Perfect forwarding', 'Passing an argument through generic code while preserving whether the original caller supplied an lvalue or rvalue and preserving cv-qualification.', 'emplace constructs an element with the caller’s original argument categories.'),
    concept('std::forward', 'A conditional cast used with a deduced forwarding-reference type to restore the caller’s original value category.', 'std::forward<T>(value) remains an lvalue when T is U& and becomes an rvalue when T is U.'),
  ],
  'cpp-overload': [
    concept('Overload set', 'The visible functions sharing a name that are considered as candidates for a call.', 'print(int), print(double), and a constrained print<T> form one overload set.'),
    concept('Viable function', 'A candidate whose parameter count, conversions, access, and constraints allow it to accept the supplied arguments.', 'A template removed by a failed requires-clause is not viable.'),
    concept('Conversion sequence', 'The ordered conversions needed to match an argument to a parameter; exact matches rank ahead of promotions and broader conversions.', 'An int literal selects f(int) ahead of f(long).'),
    concept('User-defined conversion', 'A conversion supplied by a converting constructor or conversion operator and considered after standard conversions.', 'A StringView constructor can permit a string literal to match a function expecting StringView.'),
    concept('Ambiguous call', 'A call for which overload resolution finds viable candidates but cannot identify one unique best function.', 'f(long) and f(double) can make f(1) ambiguous when neither conversion is better.'),
  ],
  'cpp-operators': [
    concept('Operator overloading', 'Defining the behavior of an existing C++ operator for a user-defined type while keeping the operator’s fixed precedence, associativity, and arity.', 'Complex operator+(Complex, Complex) returns the mathematical sum.'),
    concept('Member operator', 'An overloaded operator implemented as a member function whose left operand is the current object.', 'buffer[index] naturally uses a member operator[].'),
    concept('Non-member operator', 'An overloaded operator implemented as a free function, often enabling symmetric conversions for both operands.', 'operator+(Duration, Duration) can be a non-member found by argument-dependent lookup.'),
    concept('Comparison operators', 'Equality and ordering operations that must form a coherent relation; C++20 can synthesize related comparisons from operator<=> and operator==.', 'Sorting assumes the comparison is transitive and irreflexive.'),
    concept('Stream operator', 'A non-member operator<< or operator>> that integrates a type with stream insertion or extraction while preserving stream state.', 'operator<<(std::ostream&, const Packet&) returns the same stream for chaining.'),
  ],
  'cpp-inheritance': [
    concept('Inheritance', 'A relationship in which a derived object contains a base-class subobject and may extend or specialize the base contract.', 'A UartSensor may derive from an abstract Sensor interface.'),
    concept('Public inheritance', 'Inheritance that preserves the base public interface and models substitutability: every derived object should satisfy the base contract.', 'A Circle publicly deriving from Shape must obey Shape’s draw contract.'),
    concept('protected member', 'A base member accessible to derived classes; protected mutable state creates representation coupling and should be used sparingly.', 'A protected customization hook is safer than exposing a protected container.'),
    concept('Diamond inheritance', 'A graph where one most-derived class reaches the same base through two inheritance paths, potentially creating two base subobjects and ambiguity.', 'PoweredSensor inheriting through both Sensor and Device branches can contain two Component bases.'),
    concept('Virtual inheritance', 'An inheritance form that makes all virtual paths share one base subobject in the most-derived object, resolving the duplicate-base part of a diamond.', 'Both branches virtually inherit Component so the final object contains one Component.'),
  ],
  'cpp-polymorphism': [
    concept('Runtime polymorphism', 'Calling a virtual function through a base reference or pointer and selecting the override belonging to the object’s dynamic type.', 'Shape& referring to Circle dispatches draw() to Circle::draw.'),
    concept('vtable and vptr', 'A common implementation model where a polymorphic object stores a hidden pointer to a table of virtual-function targets. The standard specifies behavior, not this exact layout.', 'A virtual call loads the object’s table pointer and then the selected function entry.'),
    concept('override', 'A specifier requiring a derived virtual function to override a base virtual function, catching signature mistakes at compile time.', 'void draw() const override rejects an accidental missing const mismatch.'),
    concept('final', 'A specifier preventing further overriding of a virtual function or further derivation from a class.', 'A final safety-critical command handler cannot be replaced in a later subclass.'),
    concept('Object slicing', 'The loss of a derived object’s extra state and dynamic type when it is copied into a base object by value.', 'Base copy = derived stores only the Base subobject; virtual calls on copy use Base behavior.'),
    concept('Virtual destructor', 'A destructor that dispatches through the dynamic type so deleting through a base pointer destroys the complete derived object.', 'An interface intended for owning deletion normally declares virtual ~Interface() = default.'),
  ],
  'cpp-composition': [
    concept('Composition', 'Building a type from member objects whose lifetimes are contained inside the owning object.', 'A Logger contains a File and Formatter rather than inheriting from them.'),
    concept('Class invariant', 'A condition that must hold after construction and after every public operation finishes.', 'A ring buffer keeps size less than or equal to capacity and indices within bounds.'),
    concept('Has-a relationship', 'A design relationship where one object owns or uses another as a component rather than claiming substitutability.', 'A Car has an Engine; a Car is not a kind of Engine.'),
  ],
  'cpp-raii': [
    concept('RAII', 'Resource acquisition is initialization: ownership is stored in an object whose constructor establishes the resource and whose destructor releases it.', 'A File object closes its descriptor when scope exits.'),
    concept('Scope-bound lifetime', 'The guarantee that automatic objects are destroyed when control leaves their scope through return, exception, break, or normal fallthrough.', 'A lock_guard unlocks a mutex on every exit path.'),
    concept('Ownership', 'The responsibility for releasing a resource exactly once; an owning type makes that responsibility visible in its operations.', 'A move-only Descriptor transfers ownership and disables copying.'),
  ],
  'cpp-smart-pointers': [
    concept('unique_ptr', 'A move-only smart pointer representing one owning handle to a dynamically allocated object or array.', 'unique_ptr<Device> transfers exclusive ownership into a controller.'),
    concept('shared_ptr', 'A smart pointer whose control block counts strong owners and destroys the managed object when the final strong owner disappears.', 'Several asynchronous operations may share one immutable request state.'),
    concept('weak_ptr', 'A non-owning observer of a shared_ptr control block that can be locked temporarily without extending lifetime permanently.', 'A child keeps a weak back-reference to its parent.'),
    concept('make_unique', 'A factory that allocates and constructs an object and immediately returns a unique_ptr, avoiding a separate naked owning pointer.', 'auto buffer = std::make_unique<Buffer>(1024).'),
    concept('make_shared', 'A factory that commonly allocates the managed object and control block together and returns a shared_ptr.', 'auto state = std::make_shared<State>(initial).'),
    concept('use_count', 'The number of current shared_ptr owners reported by a control block; it is primarily diagnostic and is not a safe synchronization decision.', 'use_count() can change concurrently and should not decide whether mutation is safe.'),
    concept('Cyclic reference', 'A cycle of shared_ptr owners that keeps every strong count above zero after outside owners disappear, leaking the cycle.', 'A parent and child must not hold owning shared_ptr references to each other; make one direction weak_ptr.'),
  ],
  'cpp-exceptions': [
    concept('try block', 'A region whose thrown exceptions are matched against the catch handlers that immediately follow it.', 'A parser boundary may try a load and translate known failures.'),
    concept('throw expression', 'An operation that creates or propagates an exception and transfers control to the nearest matching active handler.', 'throw ParseError{line} abandons the current normal return path.'),
    concept('catch handler', 'A typed handler selected when the thrown object can bind to its parameter; control continues after the entire handler sequence.', 'catch (const ParseError& error) handles parse failures without slicing.'),
    concept('Multiple catch handlers', 'An ordered set of typed handlers checked from top to bottom; specific derived exceptions must appear before broader base handlers.', 'Catch Timeout before the general std::exception handler.'),
    concept('catch (...) / catch-all', 'A catch-all handler that matches any exception type and is used only when the boundary can safely clean up, translate, or terminate.', 'A thread entry boundary may record an unknown failure before stopping the worker.'),
    concept('noexcept', 'A function contract stating that no exception may escape. Violating it calls std::terminate; it also affects optimization and move selection.', 'A noexcept move constructor lets vector relocate elements by moving them.'),
    concept('Stack unwinding', 'The process of leaving active scopes after a throw and destroying fully constructed automatic objects until a matching handler is reached.', 'RAII closes files and unlocks mutexes while the stack unwinds.'),
    concept('Basic exception guarantee', 'After failure, invariants remain valid and resources do not leak, although observable state may have changed.', 'A container remains usable after an insertion allocation fails.'),
    concept('Strong exception guarantee', 'After failure, the operation has no observable effect, often implemented by building new state before committing it.', 'Copy-and-swap leaves the destination unchanged if copying fails.'),
    concept('No-throw guarantee', 'The operation promises not to fail by throwing and is essential for destructors and rollback paths.', 'A destructor must release its resource without allowing an exception to escape.'),
  ],
  'cpp-templates': [
    concept('Function template', 'A family of functions generated from type or value template arguments when a matching specialization is needed.', 'template<class T> void swap_values(T&, T&) works for many swappable types.'),
    concept('Class template', 'A family of class definitions parameterized by types or values, from which the compiler forms distinct concrete class specializations.', 'Stack<int> and Stack<Message> are distinct generated class specializations.'),
    concept('Template argument deduction', 'The compile-time process that infers template arguments from function arguments or class-template deduction guides.', 'push(value) can deduce T from value’s type.'),
    concept('Template instantiation', 'The creation of a concrete declaration or definition from a template and a specific argument list.', 'Using Stack<int> causes the compiler to instantiate the needed Stack<int> members.'),
    concept('Generic stack', 'A class template implementing last-in-first-out storage for any element type satisfying its operations.', 'Stack<Packet> pushes and pops Packet objects without rewriting stack logic.'),
    concept('Generic queue', 'A class template implementing first-in-first-out storage independently of the stored element type.', 'Queue<Event> retains arrival order for Event objects.'),
    concept('Generic swap', 'A function template that exchanges two values using their move operations while preserving their types.', 'swap_values(left, right) moves through one temporary of the deduced type.'),
    concept('Concept', 'A named compile-time predicate expressing the syntactic and semantic requirements a template argument must satisfy.', 'std::integral<T> rejects floating-point register values at the call boundary.'),
  ],
  'cpp-template-specialization': [
    concept('Full specialization', 'A replacement template definition selected for one exact set of template arguments instead of the primary template definition.', 'Serializer<bool> can encode boolean values with a dedicated representation.'),
    concept('Partial specialization', 'A class or variable template definition selected for a pattern of arguments while leaving some arguments variable; function templates cannot be partially specialized.', 'Storage<T*> defines pointer-specific behavior for every pointed-to type T.'),
    concept('Variadic template', 'A template with a parameter pack representing zero or more types or values that may be expanded in a controlled pattern.', 'make_record(1, "ok", 3.5) deduces three argument types.'),
    concept('Fold expression', 'Syntax that applies a binary operator across a parameter pack with a defined association and optional initial value.', '(0 + ... + values) adds every argument to zero.'),
    concept('Explicit instantiation', 'A declaration or definition that controls where a template specialization is generated, reducing duplicate work or exposing compiled template code.', 'template class Queue<Message>; emits that specialization in one source file.'),
  ],
  'cpp-constexpr': [
    concept('constexpr variable', 'A variable that must be initialized by a constant expression and is itself const.', 'constexpr std::size_t capacity = 64 creates a compile-time capacity.'),
    concept('constexpr function', 'A function that may participate in constant evaluation when called with constant inputs and allowed operations, while still being callable at runtime.', 'constexpr square(6) can initialize an array bound or run with a runtime value.'),
    concept('consteval function', 'An immediate function for which every potentially evaluated call must occur during compilation.', 'consteval validate_pin(5) rejects an invalid pin before the program is built.'),
    concept('Type trait', 'A template that computes or exposes a property or transformed type at compile time.', 'std::is_trivially_destructible_v<T> reports a destruction property.'),
    concept('Type-safe utility', 'A library type or compile-time constraint that represents intent in the type system rather than through unchecked integers, void pointers, or sentinels.', 'enum class, span, optional, and variant make invalid combinations harder to express.'),
  ],
  'cpp-stl': [
    concept('Sequence container', 'A container whose elements occupy a sequence, such as vector, deque, list, array, or forward_list, with different locality and invalidation rules.', 'vector provides contiguous storage and amortized constant-time push_back.'),
    concept('Ordered associative container', 'A tree-based set or map maintaining keys in comparison order with logarithmic lookup and update.', 'map supports ordered traversal and lower_bound.'),
    concept('Unordered associative container', 'A hash-table set or map offering expected constant-time lookup without sorted iteration.', 'unordered_map requires a hash and equality relation that agree.'),
    concept('Container adaptor', 'A restricted interface such as stack, queue, or priority_queue built over an underlying container.', 'priority_queue exposes only the highest-priority element.'),
    concept('Algorithm', 'A standard operation such as sort, find, transform, copy, or accumulate that works through iterators or ranges rather than owning storage.', 'std::ranges::sort(records, {}, &Record::id) orders records by id.'),
    concept('Function object', 'An object callable with function-call syntax, commonly a class with operator(), which may carry configuration state.', 'std::less<> is a reusable comparison function object.'),
    concept('std::function', 'A type-erased owning wrapper for a copyable callable with one declared function signature; it trades allocation and indirect-call overhead for runtime flexibility.', 'std::function<void(int)> can store a function pointer, lambda, or compatible function object behind one interface.'),
  ],
  'cpp-iterators': [
    concept('begin and end', 'Functions returning the first position and the one-past-last sentinel or iterator of a range.', 'Algorithms process [begin(values), end(values)); end is never dereferenced.'),
    concept('cbegin and cend', 'Functions returning read-only iterators even when the original container is mutable.', 'std::cbegin(values) prevents mutation through the iterator.'),
    concept('rbegin and rend', 'Functions returning reverse iterators spanning a sequence from its last element toward its first.', 'The reverse range is [rbegin(values), rend(values)).'),
    concept('Iterator category', 'A capability contract describing whether an iterator supports one-pass reading, repeated traversal, backward movement, random access, or contiguous storage.', 'std::sort requires random-access iterators and therefore rejects list iterators.'),
    concept('Iterator invalidation', 'The loss of validity of iterators, pointers, or references after a container operation changes storage or structure.', 'Vector reallocation invalidates every iterator into the old allocation.'),
    concept('Range', 'A beginning plus an ending condition that defines which elements an algorithm may visit.', 'A filtered view is a lazy range whose source must remain alive.'),
  ],
  'cpp-lambdas': [
    concept('Lambda expression', 'Syntax that creates an unnamed function object with a capture list, parameter list, optional specifiers and return type, and a function body.', '[factor](int value) { return value * factor; } creates a callable object.'),
    concept('Capture by value', 'Copying a named local into the lambda object so later calls use the captured value independently of the original variable.', '[threshold] remains valid after the creating scope if threshold is copyable.'),
    concept('Capture by reference', 'Storing access to an existing local object; the referenced object must outlive every lambda invocation.', '[&total] can update total synchronously but dangles if the callback escapes its scope.'),
    concept('Generic lambda', 'A lambda whose auto parameters make its generated call operator a function template with one specialization per used argument shape.', '[](const auto& value) { return value.size(); } accepts several sized types.'),
    concept('Mutable lambda', 'A value-capturing lambda marked mutable so its non-const call operator may modify its captured copies.', '[count = 0]() mutable { return ++count; } keeps private counter state.'),
    concept('Lambda with algorithm', 'Passing a lambda as the callable that defines an algorithm’s predicate, transformation, projection, or action.', 'std::ranges::count_if(values, [](int value) { return value > 0; }) counts positives.'),
  ],
  'cpp-errors': [
    concept('std::optional', 'A value type containing either one T or no value, suitable for expected absence that needs no detailed error.', 'find_user may return nullopt when no user matches.'),
    concept('std::variant', 'A type-safe tagged union containing exactly one alternative from a fixed list of types.', 'variant<Reading, Timeout, Disconnected> represents three valid outcomes.'),
    concept('std::visit', 'A function that invokes a visitor with the currently active alternative of one or more variants.', 'visit(overloaded handlers, result) forces handling based on the active result type.'),
    concept('std::any', 'A type-erased container for one copyable value of an open-ended runtime type; retrieving the wrong type throws bad_any_cast.', 'A plugin metadata map may use any when the set of value types is not closed.'),
    concept('std::expected', 'A C++23 value type containing either the promised value or a typed error that callers inspect explicitly.', 'parse_int returns expected<int, ParseError>.'),
  ],
  'cpp-testing': [
    concept('Unit test', 'An executable check of one small contract with controlled inputs and observable results.', 'A Buffer test verifies size and contents after a move.'),
    concept('Sanitizer', 'Compiler instrumentation that detects classes of runtime violations such as invalid memory access, undefined behavior, leaks, or data races.', 'AddressSanitizer reports a use-after-free at the invalid access.'),
    concept('Fixture', 'Controlled setup and cleanup shared by related tests without allowing mutable state to leak between cases.', 'Each filesystem test receives a fresh temporary directory.'),
    concept('Property test', 'A test of an invariant over many generated inputs rather than a single expected example.', 'Decoding arbitrary bytes must never read outside the supplied span.'),
  ],
  'cpp-concurrency': [
    concept('std::thread', 'An owning handle for one thread of execution that must be joined or detached before destruction.', 'A thread runs acquisition work and is joined before referenced state dies.'),
    concept('std::jthread', 'A C++20 joining thread whose destructor requests stop and joins, making lifetime cleanup safer.', 'A worker receives a stop_token and exits when its owner leaves scope.'),
    concept('Mutex', 'A synchronization object granting one thread at a time ownership of a compound invariant.', 'lock_guard protects a queue’s storage and size together.'),
    concept('Condition variable', 'A blocking primitive used with a mutex and predicate so threads sleep until shared state may satisfy a condition.', 'A consumer waits in a predicate loop while the queue is empty.'),
    concept('Atomic', 'An object whose supported operations are indivisible and whose memory order can establish visibility between threads.', 'A release store and acquire load publish initialized data.'),
    concept('Data race', 'Conflicting unsynchronized accesses to one memory location, at least one being a write, which makes the C++ program undefined.', 'Reading a non-atomic flag while another thread writes it is a data race.'),
  ],
};

export function cppConceptsFor(topicId) {
  const result = cppConcepts[topicId];
  if (!result) {
    throw new Error(`Missing authored C++ concepts: ${topicId}`);
  }
  return result;
}
